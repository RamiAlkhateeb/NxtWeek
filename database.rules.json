{
  "rules": {
    // WARNING: There is currently NO server-side identity verification.
    // Any visitor can read/write any household's or user's data by guessing/entering their email.
    // This is a known, accepted limitation for the current testing phase.
    ".read": "true",
    ".write": "true"
  }
}

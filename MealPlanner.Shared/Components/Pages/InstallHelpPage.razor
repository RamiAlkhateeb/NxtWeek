@page "/settings/install"
@using MealPlanner.Shared.Components
@inject NavigationManager Nav
@inject ILocalizationService Loc

<div class="app app-content-wrapper install-help-page" dir="@Loc.Direction">
    <UiPageHeader Icon="📲" Title="@Loc["install_title"]" Subtitle="@Loc["install_subtitle"]" />

    <div class="install-carousel-wrap">
        <AddToHomeCarousel @bind-Current="welcomeSlide" ShowProgress="true" FinishText="@Loc["done"]" OnFinish="GoBack" />
    </div>

    <section class="install-section">
        <div class="install-step">
            <span class="install-step-num">💡</span>
            <div>
                <h2>@Loc["install_works_title"]</h2>
                <p>@Loc["install_works_desc"]</p>
            </div>
        </div>
    </section>

    <div class="install-tip">
        <span aria-hidden="true">✨</span>
        <p>@Loc["install_tip"]</p>
    </div>
</div>

@code {
    private int welcomeSlide;
    private void GoBack() => Nav.NavigateTo("/settings");
}
